services:
  - type: web
    name: upsa-frt-attendance
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn run:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: HIK_SIMULATE
        value: "1"
